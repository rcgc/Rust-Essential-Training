/*
    * Write in console the following command:
    *
    * cargo new project_name
    *
    * Cargo manages dependencies for repeatable builds
    * Donwloads and builds external libraries
    * Calls rustc with correct parameters
    * Included with standard rustup installation
 */

// TOML = Tom's Obvious Minimal Language

fn main() {
    println!("Hello, world!");
}
